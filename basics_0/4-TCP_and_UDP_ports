#!/usr/bin/env bash
# Display listening TCP/UDP ports with PID/Program name (netstat or ss)

if command -v netstat >/dev/null 2>&1; then
  netstat -tulnp
elif command -v ss >/dev/null 2>&1; then
  ss -tulnp
else
  echo "No suitable tool found (need netstat or ss)" >&2
  exit 1
fi
